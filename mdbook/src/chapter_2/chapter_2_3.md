# Unifying the two

TODO: How binary joins and WCOJ are both instances of the same `wco_join` function. Single atom = binary join. Multiple atoms = full WCOJ machinery. No special cases in the code path.
